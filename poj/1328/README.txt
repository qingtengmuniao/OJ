 这道题本来以为很简单，却折腾了好久。

 我的思路是，先以每个小岛为圆心，以雷达射程为半径画圆，与横轴会交出一个区间。然后利用所有交出来的区间，按左边横坐标排序。然后扫描，比如第一个区间一定会占一个雷达，然后其他所有落在该区间内的可以和它共享一个雷达，注意每每加进一个区间，要根据新加进的区域收缩其右边界。
 取出下一个未被标记的边界，然后重复上面过程。
 每次取一个边界，就新按一个雷达。

 1. 边界判断
 d<=0的时候， 
 2. 忘了根据新加进的区域收缩右边界。
 3. 每次已经判断的边界，要跳过，不然会影响扫描。
 4. memset(state, sizeof(state), 0)不知道为啥不好使。因为写错啦！！
 memset(state, 0， sizeof(state))

 看看还有别的贪心思路没。
