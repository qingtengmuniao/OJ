1. 最小生成树的经典题目。完全没有拓展，本来还想用Kruskal算法和Prim算法分别实现一遍。后来一想，这是个完全图，不是稀疏图，而正好前者利用邻接矩阵+Kruskal算法是绝配。后者用邻接表+Prim，因为需要对边进行排序，如果边太多肯定效率不好。
2. 思考了下，如何表示对一个集合的划分。如果只是一个二划分，那么简单，只需要用一个相同长度的向量来标记就OK了，比如用0表示不在集合里，非0表示在集合里。如果是多维集合的话，那么就可以想想并查集的思想。
所以这里面可以省去一个used数组，因为dis[i]=0就表示在集合里了。

